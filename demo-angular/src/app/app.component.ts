import { CanvasPlugin } from 'nativescript-canvas-plugin';
console.log(new CanvasPlugin().message);
import { Component } from "@angular/core";

@Component({
    moduleId: module.id,
    selector: "ns-app",
    templateUrl: "app.component.html"
})
export class AppComponent { }
